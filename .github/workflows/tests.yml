name: PHPUnit Tests

on:
    workflow_call

jobs:
  test_services:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run PHPUnit Tests for Services
        run: |
          docker exec alievna/symfony ./vendor/bin/phpunit --testsuite=services

  test_controllers:
    runs-on: ubuntu-latest
    needs: test_services
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run PHPUnit Tests for Controllers
        run: |
          docker exec alievna/symfony:latest ./vendor/bin/phpunit --testsuite=controllers
